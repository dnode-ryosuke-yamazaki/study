name: Test Python

on:
  repository_dispatch:
    types:
      - transcript_created

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Python
        env:
          MESSAGE: ${{ github.event.client_payload.message }}
        run: python test.py
